import {ButtonsContainer, SafeAreaView} from './styles';

const ProfileScreen = () => {
  return (
    <SafeAreaView>
      <ButtonsContainer />
    </SafeAreaView>
  );
};
export default ProfileScreen;
